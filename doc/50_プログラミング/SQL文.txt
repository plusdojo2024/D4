

/* questionsテーブルの作成 */
create table questions (
questions_id	integer		auto increment,
question		verchar (1000)	not null,
users_id		integer		not null,
judge		verchar (10)	not null default ‘回答募集中’,
primary key(questions_id)

/* answersテーブルの作成 */
create table answers (
answers_id	integer		auto increment,
question_id	integer		not null,
answer		verchar (1000)	not null,
user_id		integer		not null,
primary key (answers_id)
);


/* requestテーブルの作成 */
create table request (
request_id	Integer		auto increment,
address_order 	varchar (20)	not null,
request		varchar (1000)	not null,
users_id		Integer		not null,
primary key (request_id)
);
